version: 2
jobs:
  build-apps:
    docker:
      - image: circleci/golang:1.16
    steps:
      - checkout
      - run: cd apps/svc-one && go build
      - run: cd ../client-api && go build

workflows:
  version: 2
  all:
    jobs:
      - build-apps

